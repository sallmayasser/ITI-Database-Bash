#!/bin/bash

# imports:
##########
source ./scripts/menu
source ./scripts/db_table

########################################################################################

# Initialize Parent Directory
dbs_path="$(pwd)/.DBs/"
[[ ! -e "$dbs_path" ]] && mkdir "$dbs_path"

db_menu

